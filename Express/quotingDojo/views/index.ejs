<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>quotingDojo</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
</head>
<body>
<div class="container">
    <form style="width: 500px; padding: 20px; border: solid 2px blue; border-radius: 3px;" action="/process" method="post">
        <h1>Welcome to Quoting Dojo</h1>
        Your Name: <input type="text" name="name" id=""><br>
        Your Quote: <textarea name="quote" id="" cols="30" rows="10"></textarea><br>
        <input class="btn btn-success"type="submit" value="Add my quote!">
        <a href="/quotes"><button type="button" class="btn btn-primary">Skip to quotes!</button></a>
        <a href="/quotes">Skip to quotes!</a>
    </form>

    <% if(typeof(errors) != 'undefined' ) { %>
        <% for (var x in errors) { %>
         <h3><%= errors[x].message %></h3>
        <% } %>
    <% } %>
</div>
    
</body>
</html>